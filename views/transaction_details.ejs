<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Details - MoMo Data Analysis Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #FFD700;">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/" style="color: #000000;">MoMo Data Analysis Dashboard</a>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title mb-4">Transaction Details</h5>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <div class="card mb-4">
                                    <div class="card-header" style="background-color: #FFD700; color: #000000;">
                                        <h5 class="mb-0">Transaction Details</h5>
                                    </div>
                                    <div class="card-body">
                                        <div class="mb-3">
                                            <strong>Transaction ID:</strong>
                                            <span><%= transaction.transaction_id %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Amount:</strong>
                                            <span><%= transaction.amount.toLocaleString() %> RWF</span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Fee:</strong>
                                            <span><%= transaction.fee.toLocaleString() %> RWF</span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Balance:</strong>
                                            <span><%= transaction.balance.toLocaleString() %> RWF</span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Date:</strong>
                                            <span><%= new Date(transaction.transaction_date).toLocaleString() %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Status:</strong>
                                            <span class="badge bg-<%= transaction.status === 'SUCCESS' ? 'success' : 'danger' %>"><%= transaction.status %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Phone Number:</strong>
                                            <span><%= transaction.phone_number %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Type:</strong>
                                            <span><%= types && types.find(t => t.id === transaction.type_id)?.type_name?.replace(/_/g, ' ') || 'Unknown' %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Sender:</strong>
                                            <span><%= transaction.sender || 'N/A' %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Recipient:</strong>
                                            <span><%= transaction.recipient || 'N/A' %></span>
                                        </div>
                                        <div class="mb-3">
                                            <strong>Raw Message:</strong>
                                            <div class="p-3 bg-light rounded" style="white-space: pre-wrap; max-height: 200px; overflow-y: auto; font-family: monospace;">
                                                <%= transaction.raw_message %>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="mt-4">
                            <a href="/" class="btn" style="background-color: #FFD700; color: #000000;">Back to Dashboard</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 