<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Import History - MoMo Data Analysis Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">MoMo Data Analysis Dashboard</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/import-history">Import History</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">Import History</h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Filename</th>
                                        <th>Import Date</th>
                                        <th>Status</th>
                                        <th>Records Imported</th>
                                        <th>Failed Records</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% imports.forEach(function(import) { %>
                                        <tr>
                                            <td><%= import.filename %></td>
                                            <td><%= new Date(import.import_date).toLocaleString() %></td>
                                            <td>
                                                <span class="badge bg-<%= import.status === 'success' ? 'success' : 
                                                                        import.status === 'partial' ? 'warning' : 
                                                                        'danger' %>">
                                                    <%= import.status.charAt(0).toUpperCase() + import.status.slice(1) %>
                                                </span>
                                            </td>
                                            <td><%= import.records_imported %></td>
                                            <td><%= import.failed_records %></td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 